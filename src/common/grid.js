export class GridItem {
    constructor(component, gridStyle, index) {
        this.component = component;
        this.gridStyle = gridStyle
        this.index = index
    }
}

/**
 *
 */
class Grid {
    constructor(component, gridStyle, index) {
        this.component = component;
        this.gridStyle = gridStyle
        this.index = index
    }
}
